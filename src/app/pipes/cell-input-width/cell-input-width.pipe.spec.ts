import { CellInputWidthPipe } from './cell-input-width.pipe';

describe('CellInputWidthPipe', () => {
  it('create an instance', () => {
    const pipe = new CellInputWidthPipe();
    expect(pipe).toBeTruthy();
  });
});
