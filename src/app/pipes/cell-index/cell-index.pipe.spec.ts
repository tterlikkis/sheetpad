import { CellIndexPipe } from './cell-index.pipe';

describe('CellIndexPipe', () => {
  it('create an instance', () => {
    const pipe = new CellIndexPipe();
    expect(pipe).toBeTruthy();
  });
});
